on_game_start_country = {
	events = {
		cocstart.1
		tcsstart.1
		gestart.1
	}
}

on_monthly_pulse_country = { # It also triggers when species are modified, there's no on action for that so brute force it with on_monthly_pulse_country.
	events = {
		cocstart.2
		tcsstart.2
		gestart.2
	}
}

on_tradition_picked = { # Workaround to apply psionics tradition effects to all starting species.
	events = {
		cocstart.3
		tcsstart.3
		gestart.3
		cocstart.4
		tcsstart.4
		gestart.4
	}
}
